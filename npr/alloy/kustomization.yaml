apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- namespace.yaml
- rbac.yaml
- daemonset.yaml
- service.yaml

configMapGenerator:
- name: alloy-config
  namespace: alloy
  files:
  - config.alloy