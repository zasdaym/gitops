namespace: alloy
labels:
  - includeSelectors: true
    includeTemplates: true
    pairs:
      app: alloy
resources:
- namespace.yaml
- rbac.yaml
- daemonset.yaml
- service.yaml

configMapGenerator:
- name: alloy-config
  namespace: alloy
  files:
  - config.alloy
